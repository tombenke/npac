# Config parameters for a simple console logger with debug level
config:
    logger:
        transports:
            console:
                type: console # console | file | http
                level: 'debug'
                format: 'textPlain' # text | textPlain | json
