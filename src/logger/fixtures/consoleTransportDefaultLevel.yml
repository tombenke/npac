# Config parameters for a simple console logger
config:
    logger:
        transports:
            console:
                type: console # console | file | http
                format: 'textPlain' # text | textPlain | json
